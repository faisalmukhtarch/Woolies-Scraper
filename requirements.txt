# Core (always needed)
requests

# Notifications
sendgrid          # email digests via SendGrid (optional, needs SENDGRID_API_KEY)
plyer             # desktop toast notifications (optional)

# Legacy Selenium scrape provider (Chemist Warehouse + Woolworths fallback)
# Only required if you use the scrape providers; the API providers do not need these.
selenium
beautifulsoup4

# Testing
pytest
